
float first(float x);
float last(float x);
void hello(const char *str);
void world(int x, float y);
int foo(int x);
void fakeFn(const char *str, int idx);
int bar(int x, int y);
int main() {
  int y = 102;
  int z = 3;
  char q = 'a';
  return q == '?' : y + z : 0;
}
