
void hello(const char *str);
void world(int x, float y);
int first(int x);
int last(int x);
int foo(int x);
#define GOODBYE(X) (X * 3)
int bar(int x, int y);
int main() {
  return (2 + 3) + (5 + 2);
}
