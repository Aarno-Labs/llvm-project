void hello(const char *str);
void world(int x, float y);
float first(float x);
#define BAR(X) (X * 3)
float last(float x);
int foo(int x);
int bar(int x, int y);
int main() {
  return (2 + 3) + (5 + 2);
}
