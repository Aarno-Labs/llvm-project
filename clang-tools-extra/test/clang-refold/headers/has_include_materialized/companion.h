// Sibling header found by quoted lookup ONLY from values.h's own directory.
// After materialization into the TU, this companion is no longer adjacent, so
// a source-replayed `__has_include("companion.h")` would flip to the #else arm.
